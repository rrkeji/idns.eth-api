syntax = "proto3";

package udi.worker;


message WorkerConfig{

    uint32 max_threads = 3;

    uint32 min_threads = 5;
}

message WorkerStatus{
    
}

message WorkerSetConfigRequest{
    WorkerConfig config = 1;
}